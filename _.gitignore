# Editor files
.*.swp

# Claude Code local settings
.claude/*.local.json

# Explicitly exclude META.json!
!/META.json

# Generated make files
meta.mk
control.mk

# Compiler output
*.o
*.so
.deps/

# built targets
# Note: Version-specific files (sql/*--*.sql) are now tracked in git and should be committed

# Test artifacts
results/
regression.diffs
regression.out

# Generated sql/ directory for test/build
# Created by make test-build. See README.asc for details.
test/build/sql/

# Auto-generated schedule file for test/install
# Created by make when test/install/*.sql files exist.
test/install/schedule

# test/install/*.out is rewritten by every `make test` run and never
# meaningfully compared against anything (see base.mk's test/install
# comments) -- there's no reason to commit it or track changes to it.
test/install/*.out

# Misc
tmp/
.DS_Store

# pg_tle generated files
/pg_tle/
